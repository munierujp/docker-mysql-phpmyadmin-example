# docker-mysql-phpmyadmin-example
Example of MySQL + phpMyAdmin by Docker

## Usage
```sh
# run
$ docker-compose up -d
# stop
$ docker-compose stop
# start
$ docker-compose start
# remove
$ docker-compose down
```
